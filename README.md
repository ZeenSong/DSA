# DSA
Official code for "On the Discriminability of Self-Supervised Representation Learning"
## Requirements
```
pip install torch
pip install wandb
pip install tqdm
```
## Usage

```
python -m train --help
python -m test --help
```